name: "sallen_key_lowpass"
circuit_type: "filter"
description: "Second-order Sallen-Key low-pass filter"

performance_targets:
  cutoff_frequency_hz:
    min: 140000
    max: 180000
    unit: "Hz"
  max_gain_db:
    min: -5
    max: 5
    unit: "dB"
