name: "bandpass_rlc_filter"
circuit_type: "filter"
description: "Second-order RLC band-pass filter"

performance_targets:
  center_frequency_hz:
    min: 150000
    max: 170000
    unit: "Hz"
  gain_max:
    min: -10
    max: 10
    unit: "dB"
