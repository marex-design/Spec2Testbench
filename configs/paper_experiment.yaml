experiment:
  name: nominal_28_circuit_campaign
  reproducible: true
  paper_eligible_only: true

simulation:
  allow_mock: false
  allow_recovery: true
  timeout_seconds: 60

reporting:
  save_raw_outputs: true
  save_testbenches: true
  save_logs: true
  save_metrics: true
  save_provenance: true

llm:
  enabled: false
