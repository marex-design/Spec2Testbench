# Auto-generated by Spec2TestBench - TestBenchGen
# TestBench: analogcoder_pro_p19_mixer_full_testbench
# Category: full
# Circuit: analogcoder_pro_p19_mixer

from PySpice.Spice.Netlist import Circuit
from PySpice.Unit import *
import numpy as np
import matplotlib.pyplot as plt

# Load netlist from: benchmark\p19_mixer.cir
def create_circuit():
    circuit = Circuit('analogcoder_pro_p19_mixer')
    # TODO: Include netlist file: benchmark\p19_mixer.cir
    return circuit

def run_simulation():
    circuit = create_circuit()
    simulator = circuit.simulator()
    simulator.temperature = 27.0

    analysis_0 = analysis = simulator.transient(step_time=1n, end_time=50u, start_time=0)
    analysis_1 = analysis = simulator.transient(step_time=5e-09, end_time=5e-05, start_time=0)
    analysis_2 = # Analysis fourier not yet implemented

    # Extract measurements
    results = {}
    # slew_rate: deriv(V(out))
    results['slew_rate'] = None  # TODO: extract from simulation
    # settling_time: time to settle within 1%
    results['settling_time'] = None  # TODO: extract from simulation
    # thd_percent: sqrt(sum(H2^2 + H3^2 + ...))/H1
    results['thd_percent'] = None  # TODO: extract from simulation
    # fundamental_frequency: FFT fundamental frequency
    results['fundamental_frequency'] = None  # TODO: extract from simulation

    return results

if __name__ == '__main__':
    results = run_simulation()
    print('Simulation completed')
    for name, value in results.items():
        if value is not None:
            print(f'{name}: {value}')