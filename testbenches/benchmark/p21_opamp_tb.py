# Auto-generated by Spec2Testbench - TestBenchGen
# TestBench: analogcoder_pro_p21_opamp_full_testbench
# Category: full
# Circuit: analogcoder_pro_p21_opamp

from PySpice.Spice.Netlist import Circuit
from PySpice.Unit import *
import numpy as np
import matplotlib.pyplot as plt

# Load netlist from: benchmark\p21_opamp.cir
def create_circuit():
    circuit = Circuit('analogcoder_pro_p21_opamp')
    # TODO: Include netlist file: benchmark\p21_opamp.cir
    return circuit

def run_simulation():
    circuit = create_circuit()
    simulator = circuit.simulator()
    simulator.temperature = 27.0

    analysis_0 = analysis = simulator.dc(vin=(2.5, 2.5, 0.05))
    analysis_1 = analysis = simulator.ac(dec, 10, 1, 1000000000.0)
    analysis_2 = analysis = simulator.transient(step_time=1n, end_time=50u, start_time=0)

    # Extract measurements
    results = {}
    # vout_dc: V(out)
    results['vout_dc'] = None  # TODO: extract from simulation
    # quiescent_current: I(VDD)
    results['quiescent_current'] = None  # TODO: extract from simulation
    # power: VDD * I(VDD)
    results['power'] = None  # TODO: extract from simulation
    # dc_gain: 20*log10(V(out)/V(in))
    results['dc_gain'] = None  # TODO: extract from simulation
    # dc_gain_db: 20*log10(V(out)/V(in))
    results['dc_gain_db'] = None  # TODO: extract from simulation
    # bandwidth: -3 dB bandwidth
    results['bandwidth'] = None  # TODO: extract from simulation
    # unity_gain_frequency: unity gain frequency
    results['unity_gain_frequency'] = None  # TODO: extract from simulation
    # phase_margin: phase margin at unity gain
    results['phase_margin'] = None  # TODO: extract from simulation
    # slew_rate: deriv(V(out))
    results['slew_rate'] = None  # TODO: extract from simulation
    # settling_time: time to settle within 1%
    results['settling_time'] = None  # TODO: extract from simulation

    return results

if __name__ == '__main__':
    results = run_simulation()
    print('Simulation completed')
    for name, value in results.items():
        if value is not None:
            print(f'{name}: {value}')
